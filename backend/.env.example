# HTTP
PORT=4000

# Prefer DATABASE_URL when available
# DATABASE_URL=postgres://postgres:password@127.0.0.1:5432/noktasal_yazilim

# Or use discrete PG fields
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=noktasal_yazilim
PGUSER=postgres
PGPASSWORD=postgres
PGSSL=false
